# Placify Frontend Environment Settings
# This file serves as a template. Duplicate this file as '.env' and fill in your values.

# The URL of the Flask Backend Server
# Defaults to http://localhost:5000 during local offline mock development
VITE_API_URL=http://localhost:5000
